#!/usr/bin/env python
""" generated source for module MySimulator """
# package: ch.hevs.silab.structuredsim.gluecode
import java.io

import java.util.HashMap

import java.util.Map

class MySimulator(object):
    """ generated source for class MySimulator """
    @classmethod
    def run(cls, pathToInputFile, pathToResultFile):
        """ generated source for method run """
        params = HashMap()
        try:
            while (line = in_.readLine()) != None:
                params.put(key, value)
            in_.close()
            bw.write("result=" + result)
            bw.newLine()
            bw.flush()
            bw.close()
        except Exception as e:
            e.printStackTrace()

